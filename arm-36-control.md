# Arm C, the control

purba #36. No change to `sign.yml`. `Quality` is a required context and an
ordinary job reports it.

The question: after the sign job force-pushes, does `Quality` exist on the
head that merges?
